import React from "react";

const Descbox = ({title}) => {
    return (
        <div className="descBox">
            <h3 className="descTitle">{title}</h3>
        </div>
    )
}

export default Descbox;